class Solution {
public:
    bool isPalindrome(int n) {
        int temp=0;
        int original=n;
        while(n>0){
            int lastdigit= n%10;
            n= n/10;
            temp=(temp*10)+lastdigit;
        }
        return temp==original;
        
    }
};
