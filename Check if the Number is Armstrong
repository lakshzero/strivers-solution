class Solution {
public:
    bool isArmstrong(int n) {
       int sum = 0;
       int orignal = n;
        while(n>0){
            int lastdigit = n%10;
            n=n/10;
            sum=sum+(lastdigit*lastdigit*lastdigit);
        }
        return orignal==sum;

    }
};
