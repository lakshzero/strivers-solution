class Solution {
public:
    int reverseNumber(int n) {
        int temp = 0;

        while(n > 0) {
            int lastdigit = n % 10;
            n = n / 10;
            temp = (temp * 10) + lastdigit;
        }

        return temp;
    }
};
